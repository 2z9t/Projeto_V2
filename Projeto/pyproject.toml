[project]
name = "proenem-lab"
version = "2.0.0"
description = "ProENEM Lab — landing e autenticação"
requires-python = ">=3.12"
dependencies = ["flask>=3.0,<4"]

[tool.vercel]
# Vercel carrega a instância Flask `app` em app.py na raiz do Root Directory.
entrypoint = "app:app"
